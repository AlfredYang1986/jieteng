# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.JieTengService.consultation
GET     /serviceProtocol            controllers.JieTengService.serviceProtocol
GET     /consultingPage				controllers.JieTengService.consultingPage
GET     /progress					controllers.JieTengService.progress

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
